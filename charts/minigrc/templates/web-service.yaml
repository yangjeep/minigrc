apiVersion: v1
kind: Service
metadata:
  name: {{ include "minigrc.fullname" . }}-web
  labels:
    {{- include "minigrc.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minigrc.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
